-- ⚡ Hutao Hub Update Notice (v4) ⚡
local CoreGui = game:GetService("CoreGui")
local LocalizationService = game:GetService("LocalizationService")

-- Detect player country
local result, countryCode = pcall(function()
    return LocalizationService:GetCountryRegionForPlayerAsync(game.Players.LocalPlayer)
end)

-- Default language English
local message = "A new version v4 is available.\nPlease open the latest video to get the v4 script."
local buttonText = "Copy Video Link"
local videoLink = "https://youtu.be/J6u7p5jzNvg?si=iKC60pnxGHcfnn6z" -- 🔗 đổi link tại đây

-- If player is from Vietnam -> change to Vietnamese
if result and countryCode == "VN" then
    message = "Đã có phiên bản v4.\nHãy mở video này để nhận phiên bản v4 của script."
    buttonText = "Sao chép link video"
end

-- Create ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "HutaoNotice"
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = CoreGui

-- Main Frame
local Frame = Instance.new("Frame")
Frame.BackgroundColor3 = Color3.fromRGB(10, 10, 15)
Frame.BorderColor3 = Color3.fromRGB(0, 200, 255)
Frame.BorderSizePixel = 2
Frame.Position = UDim2.new(0.5, -200, 0.5, -110)
Frame.Size = UDim2.new(0, 400, 0, 220)
Frame.Parent = ScreenGui

local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 10)
Corner.Parent = Frame

-- Glow effect
local Glow = Instance.new("ImageLabel")
Glow.BackgroundTransparency = 1
Glow.Image = "rbxassetid://5028857084"
Glow.ImageColor3 = Color3.fromRGB(0, 150, 255)
Glow.ScaleType = Enum.ScaleType.Slice
Glow.SliceCenter = Rect.new(24, 24, 276, 276)
Glow.Size = UDim2.new(1, 30, 1, 30)
Glow.Position = UDim2.new(0, -15, 0, -15)
Glow.Parent = Frame

-- Logo
local Logo = Instance.new("TextLabel")
Logo.Font = Enum.Font.GothamBlack
Logo.Text = "HUTAO HUB"
Logo.TextColor3 = Color3.fromRGB(0, 200, 255)
Logo.TextSize = 32
Logo.TextStrokeTransparency = 0.7
Logo.BackgroundTransparency = 1
Logo.Position = UDim2.new(0, 0, 0.05, 0)
Logo.Size = UDim2.new(1, 0, 0, 40)
Logo.Parent = Frame

-- Notice message
local Notice = Instance.new("TextLabel")
Notice.Font = Enum.Font.GothamMedium
Notice.Text = message
Notice.TextWrapped = true
Notice.TextColor3 = Color3.fromRGB(200, 240, 255)
Notice.TextSize = 17
Notice.BackgroundTransparency = 1
Notice.Position = UDim2.new(0.1, 0, 0.28, 0)
Notice.Size = UDim2.new(0.8, 0, 0.5, 0)
Notice.Parent = Frame

-- Copy Button
local Button = Instance.new("TextButton")
Button.Font = Enum.Font.GothamBold
Button.Text = buttonText
Button.TextColor3 = Color3.fromRGB(255, 255, 255)
Button.TextSize = 16
Button.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
Button.Position = UDim2.new(0.3, 0, 0.78, 0)
Button.Size = UDim2.new(0.4, 0, 0.15, 0)
Button.Parent = Frame

local BtnCorner = Instance.new("UICorner")
BtnCorner.CornerRadius = UDim.new(0, 6)
BtnCorner.Parent = Button

-- Button click (copy link)
Button.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard(videoLink)
    elseif toclipboard then
        toclipboard(videoLink)
    else
        print("Copy not supported, link: "..videoLink)
    end
    Button.Text = "✔ Copied!"
    task.wait(2)
    Button.Text = buttonText
end)

-- Glow animation
task.spawn(function()
    while ScreenGui.Parent do
        Glow.ImageTransparency = 0.6 + math.sin(tick() * 2) * 0.1
        task.wait(0.05)
    end
end)
